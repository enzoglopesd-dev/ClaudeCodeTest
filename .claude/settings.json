{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git add -A && git diff --cached --quiet || git commit -m \"auto: save session changes [$(date +%Y-%m-%d)]\" && git push origin HEAD",
            "timeout": 30,
            "statusMessage": "Saving and pushing to GitHub..."
          }
        ]
      }
    ]
  }
}
